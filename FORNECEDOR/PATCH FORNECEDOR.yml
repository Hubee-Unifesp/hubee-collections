info:
  name: PATCH FORNECEDOR
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/fornecedores/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
